{
  "name": "secret_exposure", "version": "1.0.0",
  "description": "Detect credential-like material without an LLM judge.",
  "executor": "validator", "executor_config": {"name": "secret_exposure"},
  "input_schema": {"type": "object", "required": ["text"], "properties": {"text": {"type": "string"}}},
  "output_schema": {"type": "object", "required": ["audit_name", "audit_version", "status", "score", "label", "explanation", "evidence", "metadata", "error", "started_at", "ended_at"]},
  "tags": ["safety", "deterministic"]
}
